{
  "name": "cryptotracker",
  "version": "2.0.0",
  "description": "Crypto market + Polymarket scanner with TA signal engine (entry/TP/SL) and terminal-style web dashboard",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "deploy": "npx wrangler pages deploy public --project-name cryptotracker --commit-dirty=true"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@polymarket/clob-client": "^5.8.1",
    "express": "^4.19.2"
  }
}
